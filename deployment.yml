apiVersion: apps/v1
kind: Deployment
metadata:
  name: resale-dev-customer-front-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resale-dev-customer-front-app
  template:
    metadata:
      labels:
        app: resale-dev-customer-front-app
    spec:
      containers:
        - name: resale-dev-customer-front-app
          image: de.icr.io/dev-registry/loveresale-customer-frontend:latest
          imagePullPolicy: Always
          # env:
          #   - name: BANK_URL
          #     valueFrom:
          #       configMapKeyRef:
          #         name: backend-properties
          #         key: BANK_URL
          #   - name: CHATBOT_ID
          #     valueFrom:
          #       configMapKeyRef:
          #         name: backend-properties
          #         key: CHATBOT_ID
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: 1000Mi
              cpu: 250m
            requests:
              memory: 200Mi
              cpu: 125m
      dnsPolicy: Default

---
apiVersion: v1
kind: Service
metadata:
  name: resale-dev-customer-front-service-cip
  labels:
    app: resale-dev-customer-front-app
spec:
  type: ClusterIP
  ports:
    # the port that this service should serve on
    - port: 80
      targetPort: 80
  # label keys and values that must match in order to receive traffic for this service
  selector:
    app: resale-dev-customer-front-app
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: resale-dev-customer-front-service
#   labels:
#     app: resale-dev-customer-front-service
# spec:
#   type: NodePort
#   ports:
#     # the port that this service should serve on
#     - port: 80
#       targetPort: 8025
#   # label keys and values that must match in order to receive traffic for this service
#   selector:
#     app: resale-dev-customer-front-app

# ---
# apiVersion: networking.k8s.io/v1beta1
# kind: Ingress
# metadata:
  #   name: resale-dev-customer-front-ingress
# spec:
#   rules:
#   - host: resale-dev-customer-front-app.cluster-ingress-subdomain
#     http:
#       paths:
#       - backend:
#           serviceName: vso-dev-customer-front-service-cip
#           servicePort: 80
#         path: /
#   tls:
#   - hosts:
#     - vso-dev-customer-front-app.cluster-ingress-subdomain
#     secretName: cluster-ingress-secret
